> **⚠️ Deprecated:** This tap is no longer maintained. Install the Drone CLI from Homebrew core instead:
>
> ```
> brew install drone-cli
> ```
>
> If you previously installed from this tap, switch with:
>
> ```
> brew uninstall drone
> brew untap drone/drone
> brew install drone-cli
> ```

# homebrew-drone

Homebrew repository to install the Drone command line utility

```sh
brew tap drone/drone
brew install drone
```
